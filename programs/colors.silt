type Color {
  Red,
  Green,
  Blue,
  Yellow,
  Cyan,
  Purple
}

fn is_primary(c) {
  match c {
    Red | Blue | Yellow -> true
    _ -> false
  }
}

fn main() {
  [Red, Blue, Cyan]
  |> list.each { c -> println("{c}: {is_primary(c)}") }
}
